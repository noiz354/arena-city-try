# BUG REPORT — CITY RUSH GTA-LIKE GAME

Generated by code-reviewer + systematic-debugging skills.

---

## CRITICAL (3) — Game Won't Work

| # | File:Line | Issue | Category |
|---|-----------|-------|----------|
| 1 | `src/main.ts:76` | `game.start()` never called — game stuck on loading screen forever | missing-call |
| 2 | `src/ui/hud.ts:216` | `getElementById('hud-chunk')!` — element doesn't exist in innerHTML template, crashes every second when FPS counter updates | null-ref |
| 3 | `src/systems/EnemySystem.ts:318-325` | `removeEnemy` splices `enemies` and `spawnPoints` at same index — arrays desync after cop add/remove cycles, wrong enemy respawns at wrong location | logic-error |

## HIGH (8) — Broken Features

| # | File:Line | Issue | Category |
|---|-----------|-------|----------|
| 4 | `src/systems/WeaponSystem.ts:89` | `currentState` uses `this.ammo.get(this.currentWeaponId)!` — non-null assertion crashes on invalid weapon ID from corrupted save | null-ref |
| 5 | `src/systems/ChunkManager.ts:238-245` | `makePropMaterials()` creates 3 `MeshStandardMaterial` per chunk but never stores them in `chunk.materials` — unbounded GPU memory leak on every chunk cycle | resource-leak |
| 6 | `src/systems/VehicleManager.ts:110-112` | `dispose()` clears array but never calls `scene.remove()` or disposes geometry/material — all vehicle GPU resources leak | resource-leak |
| 7 | `src/systems/TrafficSystem.ts:178-179` | Same pattern — `dispose()` clears array without GPU cleanup, traffic car resources leak | resource-leak |
| 8 | `src/utils/InputManager.ts:71 vs 80` | `attach()` adds `mousedown` to `target` element, `detach()` removes from `window` — listener on target never removed, permanent event leak | event-listener-leak |
| 9 | `src/systems/ModeController.ts:112-123` | Single E press fires both mission start AND vehicle enter/exit in same frame — `wasPressed` doesn't consume the key, both conditions match | logic-error |
| 10 | `src/systems/MissionSystem.ts:180-183` | Assassination mission indexes enemy by static `targetId: 3` — breaks after array mutations from cop add/remove; respawned target makes mission uncompletable | logic-error |
| 11 | `src/systems/EnemySystem.ts:135` | `lastAttacked` set to `true` when enemy melees but never reset to `false` — hit double-counted or missed every frame | logic-error |

## MEDIUM (25) — Edge Cases / Crashes

| # | File:Line | Issue | Category |
|---|-----------|-------|----------|
| 12 | `src/systems/ParticleSystem.ts:63-64` | Pool exhaustion (140 particles) silently drops particles — 2 simultaneous explosions empty the pool | logic-error |
| 13 | `src/systems/CameraRig.ts:25,54` | `followYaw` set once in `onEnterVehicle` but never updated as vehicle turns — chase cam stays at initial heading | logic-error |
| 14 | `src/systems/MissionSystem.ts:186-188` | If chase target despawns, mission auto-completes as "consolation" — player gets full reward for doing nothing | logic-error |
| 15 | `src/systems/MissionSystem.ts:99-103` | Chase mission uses `Math.random()` while rest of game uses `seededRng` — inconsistent randomness | design-smell |
| 16 | `src/systems/MissionSystem.ts:262-269` | `rebuildMarkers` removes old marker Groups but never disposes their materials/geometries — GPU leak on each mission | resource-leak |
| 17 | `src/systems/PickupSystem.ts:92-103` | Taken pickups never removed from `this.pickups` array — grows monotonically, iterated every frame | resource-leak |
| 18 | `src/systems/PedestrianSystem.ts:89,114` | `runOver()` sets `rotation.x = -0.5` but never resets for living pedestrians — dazed pedestrians walk with permanent lean | logic-error |
| 19 | `src/game/Vehicle.ts:126-131` | Impact damage uses `lastCollided` from previous frame — fast-moving vehicles that clip through thin walls miss collision damage | logic-error |
| 20 | `src/systems/PostFX.ts:53-58` | If exception between `applyShake`/`restoreShake`, camera position permanently offset | logic-error |
| 21 | `src/systems/AudioManager.ts:238-249` | Engine re-entry within 600ms orphaned oscillator — old oscillator never stopped | resource-leak |
| 22 | `src/systems/ChunkManager.ts:58,200-201` | Shared window texture disposed while active materials still reference it — buildings show black faces | use-after-free |
| 23 | `src/systems/WantedSystem.ts:93` | `nearestRoad()` crashes on empty road arrays | null-ref |
| 24 | `src/systems/WeatherSystem.ts:81-82` | Fog mutated by both WeatherSystem and DayNightSystem without coordination — visual fighting | race-condition |
| 25 | `src/systems/TrafficSystem.ts:39-41` | `turn()` sets wrong `lane` value for post-turn intersection lookups — erratic AI at consecutive intersections | logic-error |
| 26 | `src/systems/TrafficSystem.ts:133-143` | Blocked-check is lane-unaware — adjacent lane car incorrectly blocks | logic-error |
| 27 | `src/systems/PedestrianSystem.ts:77,88` | `panicSource: new Vector3()` — flee direction is toward world origin (0,0,0), not actual damage source | logic-error |
| 28 | `src/systems/PedestrianSystem.ts:102` | `speechCooldown` decremented by hardcoded `0.016` — frame-rate dependent, breaks at different FPS | logic-error |
| 29 | `src/game/Player.ts:199-206` | Center-inside-box push over-corrects by `penX + RADIUS`, can push player through narrow buildings | logic-error |
| 30 | `src/game/Game.ts:432-443` | `exploded` Set grows unboundedly — destroyed vehicles never removed, second explosion skipped after repair | resource-leak |
| 31 | `src/systems/DayNightSystem.ts:27` | Moon `DirectionalLight` position never set — moon light does nothing | logic-error |
| 32 | `src/systems/EnemySystem.ts:329-348` | `generateSpawnPoints` may place fewer than `ENEMY_COUNT` enemies if RNG is unlucky — respawn reads undefined | null-ref |
| 33 | `src/game/Game.ts:114-314` | 200-line constructor does too much — exception during construction leaves partial state, no cleanup | design-smell |
| 34 | `src/systems/VehicleManager.ts:30-36` | `VEHICLE_CONFIGS[0/1/2]` accessed without bounds check | null-ref |
| 35 | `src/systems/WeaponSystem.ts:146` | `deserialize()` crashes on unknown weapon ID in ammo data | null-ref |
| 36 | `src/systems/MinimapSystem.ts:20` | `getElementById('ui-root')!` null crash if DOM element missing | null-ref |
| 37 | `src/ui/hud.ts:92-216` | 20+ `document.getElementById` calls per frame — should be cached | performance |

## LOW (21) — Code Quality / Minor Leaks

| # | File:Line | Issue | Category |
|---|-----------|-------|----------|
| 38 | `src/systems/ChunkManager.ts:354-356` | `simpleInstances.geometry` disposed twice (double-dispose) | code-quality |
| 39 | `src/systems/ChunkManager.ts:58` | `sharedWindowTexture` singleton fragile with multiple ChunkManager instances | design-smell |
| 40 | `src/systems/CityGenerator.ts:89-99` | Landmark exclusion zone ±14 is larger than tower footprint ±8 — unintentional empty plaza | design-smell |
| 41 | `src/systems/CityGenerator.ts:102-103` | Iterates 9 blocks per chunk but only 4 can intersect — wasteful | performance |
| 42 | `src/systems/EnemySystem.ts:143,127` | Movement applied before collision resolution — enemies clip through walls on contact frame | logic-error |
| 43 | `src/systems/EnemySystem.ts:161` | Building collision Y-cull hardcodes enemy height at 2.2m | design-smell |
| 44 | `src/systems/EnemySystem.ts:272-278` | `alive` getter allocates new array on every access — GC pressure in hot loops | performance |
| 45 | `src/systems/MissionSystem.ts:279-288` | `deserialize` doesn't validate `done`/`started` arrays — corrupted save with `done: "string"` breaks `.includes()` | null-ref |
| 46 | `src/systems/MissionSystem.ts:143-144` | Assassination waypoint follows sinking corpse during death animation | logic-error |
| 47 | `src/game/Game.ts:81,316-318` | `updateCallbacks` Set never cleared on destroy — prevents GC of game object | resource-leak |
| 48 | `src/systems/AudioManager.ts:22-38` | `AudioContext` never closed on destroy, ambient `BufferSource` never stopped | resource-leak |
| 49 | `src/systems/AudioManager.ts:79-108` | `playExplosion()` is non-spatial duplicate of `playExplosionAt()` — API confusion | dead-code |
| 50 | `src/systems/AudioManager.ts:254-277` | `startAmbient()` creates looping source but never stores reference — can't stop later | resource-leak |
| 51 | `src/systems/WeatherSystem.ts:24-37` | Rain geometry/material never disposed on destroy | resource-leak |
| 52 | `src/systems/DayNightSystem.ts:24-29` | Strong references to scene objects prevent GC after destroy | resource-leak |
| 53 | `src/systems/MinimapSystem.ts:20,105` | Canvas appended to DOM but never removed on destroy | resource-leak |
| 54 | `src/ui/pauseMenu.ts:58` | DOM element appended but never removed on destroy | resource-leak |
| 55 | `src/ui/hud.ts:59` | DOM element appended but never removed on destroy | resource-leak |
| 56 | `src/systems/ParticleSystem.ts:114-123` | `dispose()` disposes geometry/material but never removes meshes from scene | resource-leak |
| 57 | `src/systems/ParticleSystem.ts:110` | `new Color(0x888899)` allocated per smoke particle per frame — GC pressure | performance |
| 58 | `src/utils/raycast.ts:45,72` | `center.clone().sub(origin)` allocates Vector3 per call in hot path | performance |
| 59 | `src/systems/MobileControls.ts:47-65` | Per-button touch listeners never explicitly removed on dispose | event-listener-leak |
| 60 | `src/systems/MobileControls.ts:29-33` | Orphaned DOM elements created when inactive — never appended but never GC'd | dead-code |
| 61 | `src/utils/errors.ts:63-74` | `webglcontextlost` handler stacks overlapping overlays — no dismiss mechanism | logic-error |
| 62 | `src/analytics/tracker.ts:78` | `pagehide` listener never removed — accumulates on restart | event-listener-leak |
| 63 | `src/systems/WeaponView.ts` | No `dispose()` method — all geometries/materials leak on destroy | resource-leak |
| 64 | `src/systems/PickupSystem.ts` | No `dispose()` method — pickup group and meshes leak on destroy | resource-leak |
| 65 | `src/game/Game.ts:528-531` | `restart()` calls `window.location.reload()` without calling `destroy()` first | resource-leak |
| 66 | `src/data/missions.ts:19` | `targetId: 3` is a raw array index — fragile to enemy list changes | design-smell |

---

## Summary

| Severity | Count |
|----------|-------|
| CRITICAL | 3 |
| HIGH | 8 |
| MEDIUM | 25 |
| LOW | 29 |
| **Total** | **65** |

## Fix Priority

1. **Phase 1 — Boot**: CRITICAL #1, #2 (game can't start)
2. **Phase 2 — Core Systems**: CRITICAL #3, HIGH #4-#11 (broken gameplay)
3. **Phase 3 — Memory**: HIGH #5-#7, MEDIUM resource leaks (GPU/CPU leaks)
4. **Phase 4 — Edge Cases**: MEDIUM logic errors (gameplay polish)
5. **Phase 5 — Quality**: LOW code quality and minor leaks
